{
  "name": "raven-web-dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "Unit tests for the Raven web dashboard's client-side logic. The Python package is the project; this exists only so raven/web/static has a test runner.",
  "type": "commonjs",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "vitest": "^5.0.0"
  }
}
